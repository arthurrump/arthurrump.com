---
title: Code. We need to talk about code.
category: Posts
---

Code is great.

```python
print("Hello, world! This is Python.")
# Below is an if-statement
if input("What do you say? ") != "no":
    print("Ah, that's great to hear!")
else:
    print("Oh, ok")

@route("bla")
def func(someParam, another):
    """
    Loops a loop and does nothing.
    """

    for x in another:
        if x != None and x == True:
            pass

pattern = r'\d\w*'
```

Oof, some PHP:

```php
<!DOCTYPE html>
<html>
<body>

<?php
function familyName($fname, $year) {
  echo "$fname Refsnes. Born in $year <br>";
}

familyName("Hege","1975");
familyName("Stale","1978");
familyName("Kai Jim","1983");
?>

</body>
</html>

```

Here's some HTML:
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Hello, world!</title>
</head>
<body>
    <h1 id="hello" data-something>Hello,&nbsp;world!</h1>
</body>
</html>
```

Unfortunately, JavaScript also exists.

```javascript
console.log("Hello, world!")
const pattern = /thing: \d\w*/
```

And then there are some actually great languages, like F#.

```fsharp {.numberlines startFrom="4"}
let rec fib n =
    match n with
    | 0
    | 1 -> 1
    | n -> fib (n - 1) + fib (n - 2)

let n = 5
printfn "Hello world! %d is the %dth Fibonacci number!" (fib n) n

[<Something>]
let multilineString = """
This is a multiline string!
Woohoo!
"""
```

Let's make the .NET complete:

```csharp
var x = "This is a message";
var y = @"This is a verbatim \ string";

[Something]
class Whatever {
    String Property { get; set; }
}
```

And here's a full example which covers a lot:

```c
/**
 * SPDX-FileCopyrightText: 2020 Christoph Cullmann <cullmann@kde.org>
 * SPDX-License-Identifier: MIT
 */

// BEGIN
#include <string>
#include <QString>
// END

/**
 * TODO: improve documentation
 * @param magicArgument some magic argument
 * @return magic return value
 */
int main(uint64_t magicArgument)
{
    if (magicArgument > 1) {
        const std::string string = "source file: \"" __FILE__ "\"";
        const QString qString(QStringLiteral("test"));
        return qrand();
    }

    /* BUG: bogus integer constant inside next line */
    const double g = 1.1e12 * 0b01'01'01'01 - 43a + 0x11234 * 0234ULL - 'c' * 42;
    return g > 1.3f;
}
```

Here's some C++ from [esphome-opentherm](https://github.com/arthurrump/esphome-opentherm/blob/main/components/opentherm/hub.cpp):

```cpp
#include "hub.h"

// Disable incomplete switch statement warnings, because the cases in each
// switch are generated based on the configured sensors and inputs.
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wswitch"

#define ID(x) x
#define SHOW2(x) #x
#define SHOW(x) SHOW2(x)

void OpenthermHub::dump_config() {
    ESP_LOGCONFIG(TAG, "OpenTherm:");
    ESP_LOGCONFIG(TAG, "  In: GPIO%d", this->in_pin);
    ESP_LOGCONFIG(TAG, "  Out: GPIO%d", this->out_pin);
    ESP_LOGCONFIG(TAG, "  Sensors: %s", SHOW(OPENTHERM_SENSOR_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Binary sensors: %s", SHOW(OPENTHERM_BINARY_SENSOR_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Switches: %s", SHOW(OPENTHERM_SWITCH_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Input sensors: %s", SHOW(OPENTHERM_INPUT_SENSOR_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Outputs: %s", SHOW(OPENTHERM_OUTPUT_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Numbers: %s", SHOW(OPENTHERM_NUMBER_LIST(ID, )));
    ESP_LOGCONFIG(TAG, "  Initial requests:");
    for (auto type : this->initial_messages) {
        ESP_LOGCONFIG(TAG, "  - %d", type);
    }
    ESP_LOGCONFIG(TAG, "  Repeating requests:");
    for (auto type : this->repeating_messages) {
        ESP_LOGCONFIG(TAG, "  - %d", type);
    }
}
```

And some C++ with Doxygen comments:

```cpp
/**
 * @file example.cpp
 * @brief This file demonstrates Doxygen annotations.
 */

/**
 * @brief Adds two integers.
 *
 * This function takes two integers as input and returns their sum.
 * 
 * @param a The first integer to add.
 * @param b The second integer to add.
 * @return The sum of @a a and @a b.
 * 
 * @note This function is simple and does not handle overflow.
 * 
 * @warning Ensure that the integers are within the valid range to avoid overflow.
 */
int add(int a, int b) {
    return a + b;
}
```
